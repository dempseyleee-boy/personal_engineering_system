# task_context/

This directory stores current-task context files generated by the Agent.

Expected files during task execution:

```text
CONTEXT_PACKAGE.md
TASK_CONTEXT.md
```

These files are task-specific and may be refreshed for each complex task.
